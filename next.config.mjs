/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "port-0-time-attack-fullstack-server-dc9c2nltdolfnd.sel5.cloudtype.app",
    ],
  },
};

export default nextConfig;
